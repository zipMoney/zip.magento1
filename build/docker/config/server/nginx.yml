# Default configruation for Nginx server

server:
    dir:
        root: var/www/html
        config: etc/nginx/conf.d
        ssl: etc/ssl/certs
        log: var/log/nginx
    port:
        http: 80
        https: 443